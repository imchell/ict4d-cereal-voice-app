<?xml version="1.0" encoding="UTF-8"?>
<vxml version="2.1">
  <form>
    <field name="leaving" type="digits">
      <prompt>
      Do you want another query? Press one to confirm. Press two to leave the app.
      </prompt>
      <noinput>
      Press one to confirm. Press two to leave the app.
      </noinput>
      <filled>
        <if cond="leaving == '1'"> 
        The app is being redirected to the start.
        <goto next="actions.xml" />
        <else/>
        Thanks for using. Goodbye!
        </if> 
      </filled>
    </field>
  </form>
</vxml>
